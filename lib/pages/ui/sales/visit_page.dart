import 'package:flutter/material.dart';

class SalesVisitPage extends StatelessWidget {
  const SalesVisitPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Visit'),
    );
  }
}
