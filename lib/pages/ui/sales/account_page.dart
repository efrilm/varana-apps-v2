import 'package:flutter/material.dart';

class SalesAccountPage extends StatelessWidget {
  const SalesAccountPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Account'),
    );
  }
}
